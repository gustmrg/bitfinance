# Implementation Plans

Generated by the `improve` skill on 2026-07-11. Execute plans in the order below.
Read each plan fully, honor its STOP conditions, preserve pre-existing working-tree
changes, and update the status row as execution progresses.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| 001 | Integrate all backend endpoints into frontend-v2 | P1 | L | — | BLOCKED (live smoke and phase commits unavailable in sandbox) |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` (with reason), or
`REJECTED` (with rationale).

## Dependency notes

- Plan 001 is self-contained and internally phase-gated. Complete its transport/auth
  foundation before organization, dashboard, bills, and expenses work.
- Production deployment cutover is deliberately deferred and should be planned only
  after all 39 endpoint integrations pass contract and live smoke tests.

## Findings considered and rejected

- Adding member role editing/removal: rejected because the backend exposes no such
  endpoints; fabricating the capability would make the new UI misleading.
- Adding organization timezone mutation: rejected because the current organization
  contract has no timezone field or mutation.
- Extending expense list filters in the backend: rejected from this plan because the
  request is frontend integration and backend contract changes increase scope/risk.
- Reusing the old frontend UI: rejected; only its proven API/session/query patterns
  should be adapted so the frontend-v2 redesign remains intact.
